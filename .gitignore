# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# Unanchored: services/wallet_ui has its own, 644 MB of it.
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env.local
# any hand- or tool-made copy of a secrets file. A .bak of .env.local
# carries the same live keys and was NOT covered by the line above.
.env*.bak
.env*.bak.*
.env.local.*
.env
.env.old
.env.local.old

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

python-service/.checkpoints
python-service/.cache
python-service/__pycache__/
python-service/venv*/
python-service/timesfm/venv*/
venv*/
timesfm/
**/__pycache__/
*.pyc
# Kalshi UI-uploaded credentials (never commit)
.kalshi-credentials.json

# Persistent trade log (local algo data, not for VCS)
data/trade-log.json
data/agent-config.json
data/trade-journal.json
data/trade-journal.json.tmp

# Python service runtime cache/output (generated at runtime)
python-service/_backtest_cache_*.json
python-service/live_analysis_cache.json
python-service/live_analysis_results.json
python-service/live_fills.json
python-service/research_cache.json

.sentient-trader/config.env

# Python daemon logs
logs/
python-service/logs/

# Pathia-Trader runtime state (per-strategy timers, position claims, telemetry —
# generated at runtime, never source). The bot recreates these on start.
# alpha_swarm candle dataset — 17MB data, rebuild with research/alpha_swarm/lib/build_dataset.py
research/alpha_swarm/dataset.json
research/alpha_swarm/funding.json
research/alpha_swarm/**/marginal_dataset.json
research/alpha_swarm/**/__pycache__/
.crash_continue_div_short_live_seen.json
.crash_continue_div_short_live_ts
.crash_continue_div_short_shadow.jsonl
.premium_fade_short_live_seen.json
.premium_fade_short_live_ts
.engulf_short_live_seen.json
.engulf_short_live_ts
shadow_ledger/
.data_funding_oi.jsonl
.data_logger_ts
.extreme_fade_live_ts
.extreme_fade_state.json
.hail_mary_short_ts
.rally_exhaustion_live_seen.json
.rally_exhaustion_live_ts
.rebalancer_claims.json
.xs_momentum_positions.json
.xs_rebalance_ts
.xs_volmgd_history
socket_full.json
socket_full.txt

# sentient-trader-mcp build artifacts
sentient-trader-mcp/dist/
sentient-trader-mcp/*.egg-info/

# Dev/scratch scripts
python-service/backtest_67c.py
python-service/test_tiered_kelly.py

# Scanner config
.scanner-config.json

ONE_PAGER.md
THREAD.md
DISTRIBUTE.md

# Agent config
.agent-config.json
.agent-memory.json
.dsl-state.json
.positions-snapshot.json
.monitor-journal.md

# Rust build artifacts
target/

# Editor backups
*.bak

# Local backtest output
backtest-trades.csv
.oi-history.json

# Local timestamped backups (config edits, etc.)
*.bak-*
*.bak2-*
*.bak3-*
*.bak2

# --- Pathia Python bot: runtime state / research caches / scratch (generated, not source) ---
# self-collected runtime time-series + shadow/tracker state (per-machine, never commit)
.oi-timeseries.jsonl
.volstop-shadow.json
.volstop-shadow.jsonl
.smart-money-seen.json
.smart-money-traders.json
.heartbeat.json
.agent-snapshot.json

# backtest / edge-research disk caches (regenerated on demand)
*-cache*.json

# local backup snapshots + one-off scratch
.backups/
.claude_prompt_regime.txt
.clean-slate.json

# alpha-hunt data caches
.bt_candle_cache.json
.bt_funding_cache.json
.catalyst_gdelt_cache.json

# consolidated runtime state dir (PATHIA_STATE_DIR, 2026-07-09)
.state/

# alpha_swarm re-fetchable data caches. These are downloaded candles, funding,
# filings and event tables — regenerate by rerunning the hypothesis script that
# built them (W-U1_unlock_backtest.py for the W-U set, and so on). They were
# tracked until 2026-09-01, which put 83MB of throwaway JSON in every clone; the
# three W-U/W-R entries that used to be listed here by name are covered by the
# patterns below. Findings, specs, hypothesis code and *_results.json stay
# tracked — those are the evidence, and test_evidence_doctrine reads them.
research/alpha_swarm/**/*_cache_*.json
research/alpha_swarm/**/*_cache_texts/
research/alpha_swarm/**/*_events.json
research/alpha_swarm/**/*_dataset.json

# Per-IDE agent skill copies, written by the installed caveman plugin. All three
# are byte-for-byte the same 22 files as each other and as the plugin's own
# cache, so tracking them vendored an installed plugin into a trading repo three
# times over. Worse, the plugin rewrites them on session events: on 2026-09-01
# .forge/ and .qwen/ vanished mid-session and showed up as 44 phantom deletions
# in `git status` that nobody had made. They stay on disk, the plugin owns them,
# git does not.
.agents/
.forge/
.qwen/

# Runtime dependencies for the Vercel demo deployment (api/index.py).
#
# This file exists for Vercel's Python builder, which installs from
# `requirements.txt` at the project root and nothing else. It is NOT the
# dependency set for CI or for the Docker/Fly image:
#
#   CI + Docker  ->  requirements.lock (pinned, universal, includes dev extras)
#   Vercel demo  ->  this file
#
# Kept to the runtime deps in pyproject.toml, minus the dev extras and minus
# anything only the trading loop needs. The demo serves the dashboard against
# generated data; it never opens a websocket or signs an order.
#
# eth-account is here despite the demo never signing anything: services/auth
# imports it at module scope for SIWE verification, and pathia/server.py
# imports that router unconditionally.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.27,<1
pydantic>=2.0
pydantic_settings>=2.0
python-dotenv>=1.0
hyperliquid-python-sdk>=0.23.0
eth-account>=0.13.0
msgpack>=1.1.0
prometheus-client>=0.20
